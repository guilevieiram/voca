type NavProps = {
    darkMode: boolean
}

function Nav ({darkMode}: NavProps) {
    return (
        <div className={`absolute bottom-0 left-0 w-screen h-20 bg-blue`}>

        </div>
    )
}

export default Nav